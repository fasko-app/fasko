/*
 * Represent settings of user
 */

class UserSettings {
  final int work; // minutes for work
  final int rest; // minutes for rest

  UserSettings({
    required this.work,
    required this.rest,
  });
}
